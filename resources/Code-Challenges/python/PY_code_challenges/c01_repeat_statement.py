# -----------------------------------------------------------------
# Challenge: 01_repeat_statement
#
# Prompt:
# Write a function called `repeat_statement` that takes two inputs:
#   1. `statement` (a string)
#   2. `num` (an integer)
#
# The function should return a single string where the statement is
# repeated `num` times, each on its own line.
#
# Examples:
# repeat_statement("Hello there", 1)
#   -> "Hello there"
#
# repeat_statement("Hello there", 3)
#   -> "Hello there\nHello there\nHello there"
#
# Notes:
# - Remember to add a newline character `\n` between repetitions.
# - Make sure there is no extra newline at the very end.
# -----------------------------------------------------------------

def repeat_statement(statement, num):
    return
